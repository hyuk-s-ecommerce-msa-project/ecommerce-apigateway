server:
  port: 8000

spring:
  application:
    name: gateway
  profiles:
    active: dev
  config:
    import:
      - "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"

  cloud:
    config:
      name: gameinfo,gateway